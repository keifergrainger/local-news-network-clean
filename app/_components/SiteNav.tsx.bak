'use client';
import { headers } from "next/headers";
import { getCityFromHost, cityLabel } from "@/lib/cities";

export default function SiteNav() {
  const host = headers().get("host") || "";
  const city = getCityFromHost(host);

  return (
    <nav className="mx-auto w-full max-w-6xl px-4">
      <div className="flex h-11 items-center justify-between">
        <a href="/" className="text-sm font-semibold text-gray-100 hover:text-white whitespace-nowrap">
          {cityLabel(city)} <span className="font-normal text-gray-400">&mdash; Local News &amp; Events</span>
        </a>

        <div className="flex items-center gap-1">
          <a href="/news" className="px-3 py-2 text-sm text-gray-200 rounded-md hover:bg-gray-800 whitespace-nowrap">News</a>
          <a href="/events" className="px-3 py-2 text-sm text-gray-200 rounded-md hover:bg-gray-800 whitespace-nowrap">Events</a>
          <a href="/businesses" className="px-3 py-2 text-sm text-white rounded-md bg-blue-600 hover:bg-blue-500 whitespace-nowrap">Businesses</a>
          <a href="/submit" className="px-3 py-2 text-sm text-gray-200 rounded-md hover:bg-gray-800 whitespace-nowrap">Submit</a>
          <a href="/advertise" className="px-3 py-2 text-sm text-gray-200 rounded-md hover:bg-gray-800 whitespace-nowrap">Advertise</a>
        </div>
      </div>
    </nav>
  );
}



